@model IEnumerable<ViewData.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create"></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in (IEnumerable<Product>)ViewData["ProductList"])
        {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
        }
    </tbody>
</table>
     
 <!--การรับข้อมูลจากคลาส ViewData ใน View ของไฟล์ส่วนแสดงผล Index.cshtml โดยที่
    1.ข้อมูลสินค้ายังคงอยู่ในรูปแบบเป็นชุด (@model IEnumerable<ViewBag.Models.Product)
    2.การอ่านค่าข้อมูลในคลาส ViewData ต้องทำการแปลงข้อมูลให้กลายเป็นชุดข้อมูลแบบ IEnumerable ก่อนค่าอ่าน (var item in (IEnumerable<Product>)ViewData["ProductList"]))-->
